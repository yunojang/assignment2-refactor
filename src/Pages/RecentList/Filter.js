import styled from "styled-components";

function Filter() {
  return(
    <Container>
      <BrandFilter />
    </Container>
  )
}

const Container = styled.div`
  margin-bottom: 40px;
`;

export default Filter;